
item = [
    {
    "name":"Golden Retriever",
    "price": 1500,
    "description":"a medium-to-large dog, athletic",
    },
    {
    "name":"Border Collie",
    "price": 800,
    "description":"a medium sized dog, high energy",
    },
    {
    "name":"Dachshunds",
    "price": 500,
    "description":"a small sized dog, low maintenance",
    },
    {
    "name":"German Shepherd",
    "price": 1500,
    "description":"a large sized dog, protective",
    }
]
def showDogs(item):
    for index, item in enumerate(item):
        print(index, ":", item["name"])
def buying():
    cart = []
    money = []
    showDogs(item)
    purchase = int(input("what dog would you like to purchase"))
    cart.append(item[purchase]["name"])
    money.append(item[purchase]["price"])
    ask = (input("would you like to continue?"))
    while ask == "yes":
        purchase = int(input("what dog would you like to purchase"))
        cart.append(item[purchase]["name"])
        money.append(item[purchase]["price"])
        ask = (input("would you like to continue?"))
    if ask == "no":
        print(cart)
        print(sum(money))
buying()